imperative programming
        -we need to type the code from the scratch and we need to provide every instruction
        eg.vanilla js
declarative programming
        -we use frameworks/libraries to make our work easy,we dont type it from scratch
        eg.react
legacy applications or using vanilla js uses real dom while react uses virtual dom,so what happens is as talking with the dom(which is in browser is slow),so react creates a virtual dom in the memory itself(because semi conductor changes the dom here and it is fast)....for every new change made in the react application it cfeates a new dom and compares with the previous virtual dom and makes changes